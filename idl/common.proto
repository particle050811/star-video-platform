syntax = "proto3";

package api.video.v1;

option go_package = "/platform";

// BaseResponse 表示所有接口共享的返回状态（作业给的接口示例，但是一般不这么写）
message BaseResponse {
  int32 code = 1;
  string msg = 2;
}

// CursorParams 统一游标分页请求参数
message CursorParams {
  string cursor = 1;
  int32 limit = 2;
}
