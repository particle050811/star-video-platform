version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: standard
  disable:
    - unparam
  settings:
    revive:
      enable-default-rules: true
      rules:
        - name: unused-parameter
          disabled: true
